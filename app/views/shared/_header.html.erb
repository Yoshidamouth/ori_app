<%# CSS・・・assets/stylesheets/shared/header.css %>
<header class="header">
  <div class="heading">
    <% if user_signed_in? %>
      <h1 class="logo"><%= link_to 'ORI APP', authenticated_root_path %></h1>
    <% else %>
      <h1 class="logo"><%= link_to 'ORI APP', unauthenticated_root_path %></h1>
    <% end %>
  </div>
  <div class="nav">
    <ul class='lists-right'>
      <% if user_signed_in? %>
        <% if current_user.affiliation_id == 2 %>
          <li><%= link_to '在庫一覧', inventory_path, class: 'inventory-link' %></li>
        <% elsif ![1, 2].include?(current_user.affiliation_id) %>
          <li><%= link_to '依頼する', '#', class: 'request-link' %></li>
        <% end %>
        <%= link_to "#" do %>
          <div class="avatar_container">
            <%= image_tag("user.png", alt: "User Avatar", class: "user_avatar") %>
          </div>
        <% end %>
      <% else %>
        <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
        <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
      <% end %>
    </ul>
  </div>
</header>